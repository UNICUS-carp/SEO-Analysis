# Google Tag Manager 実装ガイド
## サンヒルズWebサイト - GA4イベント設定

---

## 前提条件

✅ **Google Tag Manager (GTM-KJKF7M9) が既に導入済み**
→ GTMの管理画面にアクセスできることを確認してください

---

## 実装の流れ

### ステップ1: GTMにログイン
1. https://tagmanager.google.com/ にアクセス
2. コンテナ `GTM-KJKF7M9` を開く

### ステップ2: GA4設定タグの確認
まず、GA4タグが設定されているか確認します。

---

## 実装方法（優先度順）

## 🔴 最優先: 商品サイトへのクリック計測

### イベント1: 歩行ケアプロソックスへのクリック

#### **トリガーの作成**

1. GTM管理画面で「トリガー」→「新規」をクリック
2. トリガーの名前: `クリック - 歩行ケアプロソックス`
3. トリガーのタイプ: **リンククリック - すべての要素**
4. トリガーの発生場所: **一部のリンククリック**
5. 条件設定:
   ```
   Click URL    含む    sun-hills.info/prosocks
   ```
   または
   ```
   Click Text    含む    歩行ケアプロソックス
   ```

6. 「保存」をクリック

#### **タグの作成**

1. GTM管理画面で「タグ」→「新規」をクリック
2. タグの名前: `GA4 - 商品サイトクリック - プロソックス`
3. タグのタイプ: **Google アナリティクス: GA4 イベント**
4. 設定タグ: （既存のGA4設定タグを選択）
5. イベント名: `click_product_site`
6. イベントパラメータ:

| パラメータ名 | 値 |
|------------|---|
| product_name | 歩行ケアプロソックス |
| click_location | {{Click Element}} |
| link_url | {{Click URL}} |
| link_text | {{Click Text}} |

7. トリガー: 先ほど作成した`クリック - 歩行ケアプロソックス`を選択
8. 「保存」をクリック

#### **設定イメージ（GTM画面）**

```
タグの設定:
┌─────────────────────────────────┐
│ タグタイプ: GA4 イベント         │
│ 測定ID: G-XXXXXXXXXX            │
│ イベント名: click_product_site  │
│                                 │
│ イベントパラメータ:              │
│ product_name: 歩行ケアプロソックス│
│ click_location: {{Click Element}}│
│ link_url: {{Click URL}}         │
│ link_text: {{Click Text}}       │
└─────────────────────────────────┘

トリガー:
┌─────────────────────────────────┐
│ すべてのリンククリック           │
│                                 │
│ 条件:                           │
│ Click URL 含む                  │
│ sun-hills.info/prosocks         │
└─────────────────────────────────┘
```

---

### イベント2: おうちでできる歩行ケアへのクリック

同様の手順で設定します。

#### **トリガー**
- 名前: `クリック - おうちでできる歩行ケア`
- 条件: `Click URL 含む sun-hills.info/hoko-care`

#### **タグ**
- 名前: `GA4 - 商品サイトクリック - 歩行ケア`
- イベント名: `click_product_site`
- パラメータ `product_name`: `おうちでできる歩行ケア`

---

### イベント3: 城東整骨院サイトへのクリック

#### **トリガー**
- 名前: `クリック - 城東整骨院`
- 条件: `Click URL 含む joto-seikotsuin.com`

#### **タグ**
- 名前: `GA4 - 整骨院サイトクリック`
- イベント名: `click_clinic_site`
- パラメータ:
  - `click_location`: `{{Click Element}}`
  - `link_url`: `{{Click URL}}`
  - `link_text`: `{{Click Text}}`

---

## 🟡 重要: ブログ記事閲覧の計測

### イベント4: ブログ記事詳細の閲覧

#### **トリガー**
1. タイプ: **ページビュー - DOM Ready**
2. 名前: `ページビュー - ブログ記事詳細`
3. 条件:
   ```
   Page URL    正規表現に一致    sun-hills\.info/\d{4}/\d{2}/\d{2}/.*
   ```
   ※ ブログ記事のURL構造: `/2025/10/31/hokocare-73/`

#### **変数の作成（記事情報抽出）**

##### 変数1: 記事タイトル
1. GTM管理画面で「変数」→「新規」
2. 名前: `記事タイトル`
3. 変数のタイプ: **DOM 要素**
4. 選択方法: **CSS セレクター**
5. セレクター: `h3.entry-title a, h1.entry-title`
6. 属性名: `textContent`

##### 変数2: 記事カテゴリー
1. 名前: `記事カテゴリー`
2. 変数のタイプ: **DOM 要素**
3. セレクター: `.entry-meta .category a`
4. 属性名: `textContent`

#### **タグ**
1. 名前: `GA4 - ブログ記事閲覧`
2. イベント名: `view_blog_article`
3. パラメータ:

| パラメータ名 | 値 |
|------------|---|
| article_title | {{記事タイトル}} |
| article_category | {{記事カテゴリー}} |
| article_url | {{Page URL}} |

---

### イベント5: スクロール深度

#### **組み込み変数の有効化**
1. GTM管理画面で「変数」をクリック
2. 「組み込み変数」の「設定」をクリック
3. 以下にチェック:
   - ✅ Scroll Depth Threshold
   - ✅ Scroll Depth Units
   - ✅ Scroll Direction

#### **トリガー**
1. タイプ: **スクロール距離**
2. 名前: `スクロール深度 - 全ページ`
3. 縦方向スクロール距離: **割合** `25,50,75,90`
4. トリガーの発生場所: **すべてのページ**

#### **タグ**
1. 名前: `GA4 - スクロール深度`
2. イベント名: **scroll** (GA4標準イベント)
3. パラメータ:

| パラメータ名 | 値 |
|------------|---|
| percent_scrolled | {{Scroll Depth Threshold}} |

---

## 🟢 補助: その他のイベント

### イベント6: ナビゲーションクリック

#### **トリガー**
1. タイプ: **リンククリック**
2. 名前: `クリック - メインナビゲーション`
3. 条件:
   ```
   Click Element    CSSセレクターに一致    #primary-menu a
   ```

#### **タグ**
1. イベント名: `click_navigation`
2. パラメータ:

| パラメータ名 | 値 |
|------------|---|
| nav_item | {{Click Text}} |
| nav_type | main_navigation |
| destination_url | {{Click URL}} |

---

### イベント7: ソーシャルメディアクリック

#### **トリガー**
1. タイプ: **リンククリック**
2. 名前: `クリック - ソーシャルメディア`
3. 条件（いずれかに一致）:
   ```
   Click URL    含む    facebook.com
   または
   Click URL    含む    youtube.com
   または
   Click URL    含む    instagram.com
   ```

#### **変数: ソーシャルプラットフォーム検出**
1. 名前: `ソーシャルプラットフォーム`
2. タイプ: **カスタム JavaScript**
3. コード:
```javascript
function() {
  var url = {{Click URL}};
  if (url.indexOf('facebook.com') > -1) return 'facebook';
  if (url.indexOf('youtube.com') > -1) return 'youtube';
  if (url.indexOf('instagram.com') > -1) return 'instagram';
  return 'other';
}
```

#### **タグ**
1. イベント名: `click_social`
2. パラメータ:

| パラメータ名 | 値 |
|------------|---|
| social_platform | {{ソーシャルプラットフォーム}} |
| link_url | {{Click URL}} |

---

### イベント8: サイト内検索

#### **トリガー**
1. タイプ: **フォームの送信**
2. 名前: `フォーム送信 - 検索`
3. 条件:
   ```
   Form Classes    含む    search-form
   ```

#### **変数: 検索語の抽出**
1. 名前: `検索語`
2. タイプ: **DOM 要素**
3. セレクター: `.search-form .search-field`
4. 属性名: `value`

#### **タグ**
1. イベント名: **search** (GA4推奨イベント)
2. パラメータ:

| パラメータ名 | 値 |
|------------|---|
| search_term | {{検索語}} |

---

## デバッグとテスト

### GTMプレビューモードの使用

1. GTM管理画面で「プレビュー」をクリック
2. サイトURL `https://sun-hills.info/` を入力
3. 新しいタブでサイトが開く

### テスト手順

#### ✅ テスト1: 商品サイトクリック
1. プレビューモードでサイトを開く
2. 「歩行ケアプロソックス 公式サイトはこちら」をクリック
3. GTMデバッグパネルで以下を確認:
   - トリガー `クリック - 歩行ケアプロソックス` が発火
   - タグ `GA4 - 商品サイトクリック - プロソックス` が発火
   - イベント名: `click_product_site`
   - パラメータが正しく送信されているか

#### ✅ テスト2: ブログ記事閲覧
1. トップページからブログ記事をクリック
2. 記事詳細ページに移動
3. GTMデバッグパネルで確認:
   - タグ `GA4 - ブログ記事閲覧` が発火
   - 記事タイトルが正しく取得されているか

#### ✅ テスト3: スクロール深度
1. 記事ページを下にスクロール
2. 25%, 50%, 75%, 90% 地点で確認:
   - タグ `GA4 - スクロール深度` が発火
   - パラメータ `percent_scrolled` が正しい値か

### GA4リアルタイムレポートで確認

1. GA4管理画面を開く
2. 「レポート」→「リアルタイム」
3. 上記テストを実施
4. イベント名とイベント数が表示されるか確認

---

## 公開手順

### 1. ワークスペースの確認
全てのタグ・トリガー・変数が正しく設定されていることを確認

### 2. バージョン名の設定
- バージョン名: `GA4イベント設定 - 初期実装 (2025-11-08)`
- 説明: `商品サイトクリック、ブログ閲覧、スクロール深度などの主要イベントを設定`

### 3. 公開
1. 「送信」ボタンをクリック
2. バージョン名と説明を入力
3. 「公開」をクリック

### 4. 本番環境での確認
1. プレビューモードを解除
2. 通常のブラウザでサイトにアクセス
3. Chrome DevToolsを開く (F12)
4. ネットワークタブで `google-analytics.com/g/collect` を確認
5. イベントが送信されていることを確認

---

## トラブルシューティング

### イベントが送信されない場合

#### 問題1: トリガーが発火しない
**確認事項**:
- トリガーの条件が正しいか
- セレクターが正しい要素を指定しているか
- プレビューモードで該当要素をクリックしているか

**解決方法**:
```
Chrome DevToolsのConsoleで確認:
document.querySelector('#primary-menu a')
→ 要素が取得できるか確認
```

#### 問題2: パラメータの値が空
**原因**: 変数のセレクターが間違っている

**解決方法**:
1. GTMデバッグパネルで「Variables」を確認
2. 該当変数の値が取得できているか確認
3. セレクターを修正

#### 問題3: GA4でイベントが表示されない
**確認事項**:
- GA4測定IDが正しいか
- GA4設定タグが発火しているか
- イベント名に使用禁止文字が含まれていないか

**注意**: GA4では24時間以内のデータがリアルタイムレポートにのみ表示されます。

---

## ベストプラクティス

### 1. 命名規則の統一
```
イベント名: 小文字_アンダースコア区切り
例: click_product_site, view_blog_article

パラメータ名: 小文字_アンダースコア区切り
例: product_name, article_title
```

### 2. データレイヤーの活用（応用）
より詳細な情報を送信する場合は、データレイヤーを使用:

```html
<!-- WordPress テンプレートに追加 -->
<script>
window.dataLayer = window.dataLayer || [];
dataLayer.push({
  'article_title': '<?php echo get_the_title(); ?>',
  'article_category': '<?php echo get_the_category()[0]->name; ?>',
  'article_author': '<?php echo get_the_author(); ?>',
  'article_date': '<?php echo get_the_date('Y-m-d'); ?>'
});
</script>
```

### 3. 定期的なメンテナンス
- 月次: イベント送信状況の確認
- 四半期: KPI達成状況のレビュー
- 年次: イベント設定の見直し

---

## まとめ

### 実装すべき優先順位

#### フェーズ1（最初の1週間）
1. ✅ 商品サイトクリック（プロソックス、歩行ケア）
2. ✅ 整骨院サイトクリック
3. ✅ スクロール深度

#### フェーズ2（2週目）
4. ✅ ブログ記事閲覧
5. ✅ ナビゲーションクリック
6. ✅ ソーシャルメディアクリック

#### フェーズ3（3週目以降）
7. ✅ サイト内検索
8. ✅ その他のエンゲージメントイベント

### 期待される成果

**1週間後**:
- 商品サイトへのクリック数が可視化される
- どのページから最も誘導できているかがわかる

**1ヶ月後**:
- ブログ記事の貢献度が数値化される
- ユーザージャーニーが明確になる

**3ヶ月後**:
- データドリブンな改善施策を実施できる
- ROIが向上する

---

## 次のステップ

1. ✅ 本ガイドの内容を確認
2. → GTMにログインして実装開始
3. → プレビューモードでテスト
4. → 公開
5. → 1週間後にデータレビュー
6. → 必要に応じて追加イベントを設定

ご不明な点がございましたら、お気軽にお問い合わせください。
